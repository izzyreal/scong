package scong

object Constants {

  val scale = 4
  val deflectionThingie: Float = 10 * scale
  val tickDuration: Int = 4 / scale
  val arenaWidth: Int = 320 * scale
  val arenaHeight: Int = 240 * scale
  val canvasWidth: Int = 320 * scale
  val canvasHeight: Int = 240 * scale
  val playerWidth: Int = 4 * scale
  val playerHeight: Int = 20 * scale
  val paddleVelocity: Int = 8 * scale
  val ballSize: Int = 4 * scale
}